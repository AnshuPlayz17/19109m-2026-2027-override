name: CI

on:
  push:
  pull_request:

jobs:
  format:
    name: clang-format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install clang-format
        run: sudo apt-get update && sudo apt-get install -y clang-format

      - name: Check formatting
        run: |
          find src include \( -name '*.cpp' -o -name '*.hpp' -o -name '*.h' \) \
            -print0 | xargs -0 clang-format --dry-run --Werror

  # A compile job needs the arm-none-eabi toolchain plus the PROS CLI, and the
  # CLI does not install cleanly on current Python releases. Enable this once
  # you have a toolchain step that works, otherwise it will sit red.
  #
  # build:
  #   name: pros make
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.9'
  #     - run: sudo apt-get update && sudo apt-get install -y gcc-arm-none-eabi
  #     - run: pip install pros-cli
  #     - run: ./scripts/bootstrap.sh
  #     - run: pros make
